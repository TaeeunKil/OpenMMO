interface:
  display_name: "Simplify"
  short_description: "Review changes and apply minimal safe refactors"
  default_prompt: "Use $simplify to review these changes, apply only minimal behavior-preserving refactors, and run relevant tests."
